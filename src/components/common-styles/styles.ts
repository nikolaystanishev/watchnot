import { StyleSheet } from 'react-native';


export const commonStyles = StyleSheet.create({
  card: {
    marginBottom: 15
  },
  poster: {
    alignItems: "center",
    marginBottom: 10
  },
  centerText: {
    alignItems: "center"
  }
});
